"use client";
import{useEffect,useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import{supabase}from'@/lib/supabase';import{getEmpresaAdminAtual}from'@/lib/empresa-admin';
const PROVEDORES=[['pagamento','mercado_pago','Mercado Pago'],['pagamento','paypal','PayPal'],['pagamento','infinitepay','InfinitePay'],['pagamento','stone','Stone'],['entrega','uber_direct','Uber Direct'],['entrega','ifood','iFood'],['entrega','99_entrega','99 Entrega'],['whatsapp','meta_whatsapp','WhatsApp Oficial (Meta)']]as const;
type Pag={dinheiro:boolean;cartao_entrega:boolean;pix_manual:boolean;online:boolean};type Ent={retirada:boolean;propria:boolean;taxa:number;pedido_minimo:number;raio_km:number;uber_direct:boolean;ifood:boolean;entrega_99:boolean};const pag0:Pag={dinheiro:true,cartao_entrega:true,pix_manual:false,online:false},ent0:Ent={retirada:true,propria:false,taxa:0,pedido_minimo:0,raio_km:0,uber_direct:false,ifood:false,entrega_99:false};
export default function Integracoes(){const router=useRouter();const[empresaId,setEmpresaId]=useState(''),[empresaNome,setEmpresaNome]=useState(''),[dados,setDados]=useState<any[]>([]),[pag,setPag]=useState<Pag>(pag0),[ent,setEnt]=useState<Ent>(ent0),[msg,setMsg]=useState(''),[credStatus,setCredStatus]=useState<any[]>([]),[mpToken,setMpToken]=useState(''),[mpAmbiente,setMpAmbiente]=useState('teste'),[salvandoMp,setSalvandoMp]=useState(false);async function carregar(eid=empresaId){let id=eid;if(!id){const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/admin/login');return}const e=await getEmpresaAdminAtual(user.id);if(!e){setMsg('Sem acesso.');return}id=e.id;setEmpresaId(id);setEmpresaNome(e.nome)}const[i,p,e,cs]=await Promise.all([supabase.from('integracoes_empresa').select('*').eq('empresa_id',id),supabase.from('configuracoes_loja').select('valor').eq('empresa_id',id).eq('chave','pagamentos').maybeSingle(),supabase.from('configuracoes_loja').select('valor').eq('empresa_id',id).eq('chave','entrega').maybeSingle(),supabase.rpc('status_credencial_pagamento',{p_empresa_id:id})]);if(i.error||p.error||e.error){setMsg(i.error?.message||p.error?.message||e.error?.message||'Erro ao carregar.');return}setDados(i.data??[]);setCredStatus(cs.data??[]);setPag({...pag0,...(p.data?.valor as any||{})});setEnt({...ent0,...(e.data?.valor as any||{})})}useEffect(()=>{carregar()},[]);function item(tipo:string,provedor:string){return dados.find(x=>x.tipo===tipo&&x.provedor===provedor)}async function toggle(tipo:string,provedor:string){const atual=item(tipo,provedor);const{error}=await supabase.from('integracoes_empresa').upsert({empresa_id:empresaId,tipo,provedor,ativo:!atual?.ativo,configuracoes:atual?.configuracoes||{}},{onConflict:'empresa_id,tipo,provedor'});if(error)setMsg(error.message);else carregar(empresaId)}async function salvarOperacao(){const [a,b]=await Promise.all([supabase.from('configuracoes_loja').upsert({empresa_id:empresaId,chave:'pagamentos',valor:pag,atualizado_em:new Date().toISOString()},{onConflict:'empresa_id,chave'}),supabase.from('configuracoes_loja').upsert({empresa_id:empresaId,chave:'entrega',valor:ent,atualizado_em:new Date().toISOString()},{onConflict:'empresa_id,chave'})]);setMsg(a.error?.message||b.error?.message||'Configurações de pagamento e entrega salvas.')}
async function conectarMercadoPago(){if(!empresaId)return;setMsg('');try{const r=await fetch('/api/integracoes/mercado-pago/conectar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({empresaId})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Não foi possível iniciar a conexão.');window.location.href=d.url}catch(e:any){setMsg(e.message||'Erro ao conectar Mercado Pago.')}}
async function salvarMercadoPago(){if(!empresaId||!mpToken.trim())return setMsg('Informe o Access Token do Mercado Pago.');setSalvandoMp(true);const{error}=await supabase.rpc('salvar_credencial_pagamento',{p_empresa_id:empresaId,p_provedor:'mercado_pago',p_ambiente:mpAmbiente,p_credenciais:{access_token:mpToken.trim()}});if(!error)await supabase.from('integracoes_empresa').upsert({empresa_id:empresaId,tipo:'pagamento',provedor:'mercado_pago',ativo:true,configuracoes:{modo:'checkout_pro'}},{onConflict:'empresa_id,tipo,provedor'});setSalvandoMp(false);if(error)setMsg(error.message);else{setMpToken('');setMsg('Mercado Pago configurado com segurança.');carregar(empresaId)}}
return <main className="min-h-screen bg-neutral-50 p-4"><div className="mx-auto max-w-4xl"><Link href="/admin" className="text-sm text-brand-700">← Voltar ao painel</Link><h1 className="mt-2 text-xl font-bold">Operação e integrações · {empresaNome}</h1><p className="text-sm text-neutral-500">Configure primeiro o que a loja aceita. Provedores externos ficam separados e serão conectados às APIs posteriormente.</p>{msg&&<div className="my-4 rounded bg-white p-3 text-sm shadow-sm">{msg}</div>}
<div className="mt-5 grid gap-4 md:grid-cols-2"><section className="rounded-xl border bg-white p-4"><h2 className="font-bold">Formas de pagamento</h2><p className="mb-3 text-xs text-neutral-500">Métodos aceitos diretamente pela loja.</p><Check t="Dinheiro" v={pag.dinheiro} set={v=>setPag({...pag,dinheiro:v})}/><Check t="Cartão na entrega/retirada" v={pag.cartao_entrega} set={v=>setPag({...pag,cartao_entrega:v})}/><Check t="PIX manual" v={pag.pix_manual} set={v=>setPag({...pag,pix_manual:v})}/><Check t="Pagamento online" v={pag.online} set={v=>setPag({...pag,online:v})}/></section>
<section className="rounded-xl border bg-white p-4"><h2 className="font-bold">Entrega</h2><p className="mb-3 text-xs text-neutral-500">Regras da operação própria da empresa.</p><Check t="Retirada na loja" v={ent.retirada} set={v=>setEnt({...ent,retirada:v})}/><Check t="Entrega própria" v={ent.propria} set={v=>setEnt({...ent,propria:v})}/><div className="mt-3 grid grid-cols-3 gap-2"><Num t="Taxa R$" v={ent.taxa} set={v=>setEnt({...ent,taxa:v})}/><Num t="Pedido mín. R$" v={ent.pedido_minimo} set={v=>setEnt({...ent,pedido_minimo:v})}/><Num t="Raio km" v={ent.raio_km} set={v=>setEnt({...ent,raio_km:v})}/></div></section></div><button onClick={salvarOperacao} className="mt-4 rounded-lg bg-brand-700 px-5 py-2 text-sm font-semibold text-white">Salvar operação</button>
<h2 className="mt-8 font-bold">Pagamentos online</h2><section className="mt-3 rounded-xl border bg-white p-4"><div className="flex items-start justify-between gap-3"><div><b>Mercado Pago</b><p className="text-xs text-neutral-500">Primeira integração de pagamento online. O Access Token é armazenado em área restrita do backend.</p></div><span className={`rounded-full px-2 py-1 text-xs font-bold ${credStatus.some(x=>x.provedor==='mercado_pago')?'bg-green-100 text-green-700':'bg-neutral-100 text-neutral-500'}`}>{credStatus.some(x=>x.provedor==='mercado_pago')?'Configurado':'Não configurado'}</span></div><button onClick={conectarMercadoPago} className="mt-4 w-full rounded-lg bg-brand-700 px-4 py-3 text-sm font-bold text-white">Conectar minha conta Mercado Pago</button><details className="mt-3"><summary className="cursor-pointer text-xs text-neutral-500">Configuração manual / desenvolvimento</summary><div className="mt-3 grid gap-2 md:grid-cols-[140px_1fr_auto]"><select value={mpAmbiente} onChange={e=>setMpAmbiente(e.target.value)} className="rounded-lg border px-3 py-2 text-sm"><option value="teste">Teste</option><option value="producao">Produção</option></select><input type="password" autoComplete="off" value={mpToken} onChange={e=>setMpToken(e.target.value)} placeholder="Access Token do Mercado Pago" className="rounded-lg border px-3 py-2 text-sm"/><button onClick={salvarMercadoPago} disabled={salvandoMp||!mpToken.trim()} className="rounded-lg bg-brand-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{salvandoMp?'Salvando...':'Conectar'}</button></div><p className="mt-2 text-[11px] text-neutral-400">O token salvo nunca é retornado ao navegador depois da gravação.</p></details></section><h2 className="mt-8 font-bold">Provedores externos</h2><p className="text-xs text-neutral-500">Ativar prepara o provedor para configuração; não inicia cobranças ou entregas.</p><div className="mt-3 grid gap-3 md:grid-cols-2">{PROVEDORES.map(([tipo,provedor,nome])=>{const x=item(tipo,provedor);return <div key={provedor} className="flex items-center justify-between rounded-xl border bg-white p-4"><div><b>{nome}</b><p className="text-xs capitalize text-neutral-400">{tipo}</p></div><button onClick={()=>toggle(tipo,provedor)} className={`rounded-full px-3 py-1 text-xs font-bold ${x?.ativo?'bg-green-100 text-green-700':'bg-neutral-100 text-neutral-500'}`}>{x?.ativo?'Ativo':'Inativo'}</button></div>})}</div></div></main>}
function Check({t,v,set}:{t:string;v:boolean;set:(v:boolean)=>void}){return <label className="flex items-center justify-between border-b py-2 text-sm"><span>{t}</span><input type="checkbox" checked={v} onChange={e=>set(e.target.checked)}/></label>}function Num({t,v,set}:{t:string;v:number;set:(v:number)=>void}){return <label className="text-[11px] text-neutral-500">{t}<input type="number" min="0" step=".01" value={v} onChange={e=>set(Number(e.target.value))} className="mt-1 w-full rounded border p-2 text-sm text-neutral-900"/></label>}
