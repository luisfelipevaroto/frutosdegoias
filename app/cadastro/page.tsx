"use client";
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {supabase} from '@/lib/supabase';import {getEmpresaAtual} from '@/lib/empresa';
const KEY='delivery_cliente';type Cadastro={nome:string;cpf:string;whatsapp:string;endereco:string};const vazio:Cadastro={nome:'',cpf:'',whatsapp:'',endereco:''};
export default function CadastroPage(){const router=useRouter();const[f,setF]=useState(vazio);const[empresaId,setEmpresaId]=useState('');const[msg,setMsg]=useState('');const[salvando,setSalvando]=useState(false);useEffect(()=>{getEmpresaAtual().then(e=>setEmpresaId(e?.id||''));try{const s=localStorage.getItem(KEY);if(s)setF({...vazio,...JSON.parse(s)})}catch{}},[]);
async function salvar(e:React.FormEvent){e.preventDefault();if(!empresaId){setMsg('Empresa não identificada.');return}if(!f.nome.trim()||!f.cpf.trim()||!f.whatsapp.trim()){setMsg('Preencha nome, CPF e WhatsApp.');return}setSalvando(true);setMsg('');const{data,error}=await supabase.rpc('cadastrar_cliente',{p_empresa_id:empresaId,p_nome:f.nome,p_cpf:f.cpf,p_whatsapp:f.whatsapp,p_endereco:f.endereco||null});setSalvando(false);if(error){setMsg(error.message);return}const d=data as any;const salvo={nome:d?.nome||f.nome,cpf:d?.cpf||f.cpf.replace(/\D/g,''),whatsapp:d?.whatsapp||f.whatsapp,endereco:d?.endereco||f.endereco};localStorage.setItem(KEY,JSON.stringify(salvo));setMsg('Cadastro salvo.');setTimeout(()=>router.push('/checkout'),400)}
return <main className="mx-auto max-w-md p-4"><h1 className="mb-1 text-xl font-bold">Seu cadastro</h1><p className="mb-5 text-sm text-neutral-500">Usaremos seus dados nos pedidos e no programa de fidelidade. O endereço é necessário somente para entrega e ficará salvo para as próximas compras.</p><form onSubmit={salvar} className="space-y-3"><input required value={f.nome} onChange={e=>setF({...f,nome:e.target.value})} placeholder="Nome completo" className="w-full rounded-lg border p-3"/><input required value={f.cpf} onChange={e=>setF({...f,cpf:e.target.value})} placeholder="CPF" inputMode="numeric" className="w-full rounded-lg border p-3"/><input required value={f.whatsapp} onChange={e=>setF({...f,whatsapp:e.target.value})} placeholder="WhatsApp" inputMode="tel" className="w-full rounded-lg border p-3"/><textarea value={f.endereco} onChange={e=>setF({...f,endereco:e.target.value})} placeholder="Endereço de entrega (rua, número, complemento e bairro)" className="min-h-24 w-full rounded-lg border p-3"/><button disabled={salvando||!empresaId} className="w-full rounded-lg bg-brand-700 py-3 font-medium text-white disabled:opacity-50">{salvando?'Salvando...':'Salvar cadastro e continuar'}</button>{msg&&<p className="text-sm text-brand-700">{msg}</p>}</form></main>}
